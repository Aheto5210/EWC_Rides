# Server
PORT=3331
HOST=127.0.0.1
NODE_ENV=production

# Optional: shared “room code” (blocks API + stream without it)
# ROOM_CODE=change-me

# Tuning
MAX_PICKUP_MINUTES=10
ASSUMED_SPEED_KMH=40
MAX_ACTIVE_REQUESTS_PER_DRIVER=3
REQUEST_TTL_MINUTES=5
DRIVER_STALE_SECONDS=45
RIDER_SNAPSHOT_SECONDS=10
DRIVER_BROADCAST_MIN_MS=5000
DRIVER_BROADCAST_MIN_MOVE_M=30
ASSIGNED_TTL_MINUTES=180

# HTTPS (recommended in production; best handled by Nginx/Certbot)
# HTTPS=0
# HTTPS_PORT=3443
# HTTPS_CERT_FILE=.cert/cert.pem
# HTTPS_KEY_FILE=.cert/key.pem
